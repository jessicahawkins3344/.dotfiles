[color]
    # color opts: normal, black, red, green, yellow, blue, magenta, cyan, or white
	ui = true
    interactive = auto
	diff = auto
	status = auto
	branch = auto

[core]
    # Use custom `.gitignore` and `.gitattributes`
    excludesfile = ~/.gitignore
    attributesfile = ~/.gitattributes
	editor = subl -n -w

[credential]
	helper = osxkeychain

[diff]
    tool = mvimdiff

[difftool]
    prompt = false

[push]
    default = simple

[user]
	name = Jessica Hawkins
	email = ChristopherA@hackathon.net

